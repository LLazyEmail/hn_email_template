// partials
import { Head } from '';

const DDDisplayHead = {
  component: Head,
  params: { title, headStyles, fonts },
  display: () => {
  },
  checks: () => []
  
}

export default DDDisplayHead;
