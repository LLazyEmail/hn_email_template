// partials
import { MainComponent } from '';

const DDDisplayMain = {
  component: MainComponent,
  params: { head, body },
  display: () => {
  },
  checks: () => {
  }
  
}

export default DDDisplayMain;
