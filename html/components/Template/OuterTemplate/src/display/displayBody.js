// partials
import { Body } from '';

const DDDisplayBody = {
  component: Body,
  params: { footer, logoTop, logoBottom, content },
  display: () => {
  },
  checks: () => []
  
}

export default DDDisplayBody;
